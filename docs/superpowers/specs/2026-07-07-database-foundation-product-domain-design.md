# Design: Database Foundation & Product Domain

**Date**: 2026-07-07
**Status**: Draft — awaiting user review
**Author**: Backend Architect
**Approach**: B — Service owns Schema, Package owns Infrastructure

---

## 1. Architecture & File Structure

```
woodshop/
├── packages/database/
│   ├── package.json
│   ├── tsconfig.json
│   └── src/
│       ├── index.ts                    ← Public API exports
│       ├── database.module.ts          ← DynamicModule nhận DATABASE_URL
│       ├── prisma.service.ts           ← PrismaClient wrapper (onModuleInit/onModuleDestroy)
│       └── types.ts                    ← Shared types (TransactionClient, DbConfig)
│
├── apps/product/
│   ├── package.json
│   ├── tsconfig.json
│   ├── .env
│   ├── prisma/
│   │   ├── schema.prisma               ← Product, Category, WoodSpecies enum
│   │   └── migrations/                 ← Generated by Prisma
│   └── src/
│       ├── domain/
│       │   ├── entities/
│       │   │   ├── product.entity.ts
│       │   │   └── category.entity.ts
│       │   ├── repositories/
│       │   │   ├── product-repository.interface.ts
│       │   │   └── category-repository.interface.ts
│       │   ├── value-objects/
│       │   │   ├── dimensions.vo.ts
│       │   │   └── price.vo.ts
│       │   └── exceptions/
│       │       └── product-not-found.exception.ts
│       │
│       ├── infrastructure/
│       │   ├── database/
│       │   │   └── repositories/
│       │   │       ├── product.repository.impl.ts
│       │   │       └── category.repository.impl.ts
│       │   ├── config/
│       │   │   └── env.validation.ts
│       │   └── database.module.ts      ← Register repositories
│       │
│       └── app.module.ts               ← Wire everything together
```

**Key decisions:**
- `packages/database` chỉ chứa Prisma service + module — KHÔNG chứa schema, KHÔNG chứa repository interfaces
- `apps/product/prisma/schema.prisma` — service tự chủ schema của mình
- Repository interfaces nằm trong `domain/repositories/` — pure contracts, không phụ thuộc Prisma
- Repository implementations nằm trong `infrastructure/database/repositories/` — implement interfaces, gọi Prisma

---

## 2. Prisma Schema (`apps/product/prisma/schema.prisma`)

Domain: Đồ gỗ mỹ nghệ thành phẩm (tượng gỗ, bình phong, tranh gỗ, đồ thờ cúng...)

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Loại gỗ — enum cố định, không cần table riêng
enum WoodSpecies {
  HUONG       // Gỗ hương
  TRAC        // Gỗ trắc
  MUN         // Gỗ mun
  GO_DO       // Gỗ gõ đỏ
  CAM_LAI     // Gỗ cẩm lai
  SON_TA      // Gỗ sơn ta
  THONG       // Gỗ thông
  SUAN        // Gỗ xoan
  KHAC        // Loại khác
}

model Category {
  id          String    @id @default(uuid())
  name        String    @unique
  slug        String    @unique
  description String?
  parentId    String?   @db.Uuid
  parent      Category? @relation("CategoryHierarchy", fields: [parentId], references: [id])
  children    Category[] @relation("CategoryHierarchy")
  products    Product[]
  sortOrder   Int       @default(0)
  isActive    Boolean   @default(true)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([isActive])
  @@index([parentId])
  @@index([sortOrder])
}

model Product {
  id            String        @id @default(uuid())
  name          String
  slug          String        @unique
  description   String?
  price         Decimal       @db.Decimal(10, 2)
  currency      String        @default("VND")

  // Wood info
  woodSpecies   WoodSpecies                  // Loại gỗ (enum)

  categoryId    String?       @db.Uuid
  category      Category?     @relation(fields: [categoryId], references: [id])

  lengthCm      Decimal?      @db.Decimal(8, 2)
  widthCm       Decimal?      @db.Decimal(8, 2)
  heightCm      Decimal?      @db.Decimal(8, 2)
  weightKg      Decimal?      @db.Decimal(8, 2)

  craftsmanship String?
  artisan       String?
  finishType    String?

  sku           String?       @unique
  stockQuantity Int           @default(0)
  isOneOfAKind  Boolean       @default(false)

  images        String[]
  tags          String[]

  isActive      Boolean       @default(true)
  isFeatured    Boolean       @default(false)
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  deletedAt     DateTime?

  @@index([isActive, deletedAt])
  @@index([categoryId, isActive])
  @@index([woodSpecies, isActive])
  @@index([slug, isActive])
  @@index([isFeatured, isActive])
  @@index([price])
}
```

**Key decisions:**
- UUID cho tất cả primary keys — dễ merge data sau này, không leak internal IDs
- Soft delete (`deletedAt`) trên Product — giữ lịch sử, không xóa thật
- Category tự tham chiếu — hỗ trợ tree structure (parent/children)
- Dimensions tách riêng (`lengthCm`, `widthCm`, `heightCm`) — query theo kích thước dễ hơn
- Price là Decimal(10,2) — tránh floating point issues, max ~99 tỷ VND
- WoodSpecies là **enum** — danh sách loại gỗ cố định, không cần table riêng
- Thêm `craftsmanship`, `artisan`, `finishType` — đặc thù đồ mỹ nghệ
- Thêm `sku`, `stockQuantity`, `isOneOfAKind` — quản lý tồn kho, đánh dấu độc bản
- Không có `createdBy`/`updatedBy` — để sau khi có User service

---

## 3. Repository Layer

### 3.1 Domain Interfaces

Mỗi interface định nghĩa contract — không phụ thuộc Prisma, không phụ thuộc framework:

```typescript
// product-repository.interface.ts
export interface IProductRepository {
  findById(id: string): Promise<ProductEntity | null>;
  findBySlug(slug: string): Promise<ProductEntity | null>;
  findAll(filter: ProductFilter, pagination: PaginationParams): Promise<PaginatedResult<ProductEntity>>;
  findFeatured(limit: number): Promise<ProductEntity[]>;
  create(data: CreateProductInput): Promise<ProductEntity>;
  update(id: string, data: UpdateProductInput): Promise<ProductEntity>;
  softDelete(id: string): Promise<void>;
  findByCategory(categoryId: string, filter: ProductFilter): Promise<ProductEntity[]>;
  findByWoodSpecies(species: WoodSpecies): Promise<ProductEntity[]>;
  searchByDimensions(min: Dimensions, max: Dimensions): Promise<ProductEntity[]>;
}
```

Tương tự cho `ICategoryRepository`.

### 3.2 Infrastructure Implementation

Mỗi implementation nhận `PrismaService` qua constructor, implements interface, map Prisma types ↔ Domain entities:

```typescript
@Injectable()
export class PrismaProductRepository implements IProductRepository {
  constructor(private readonly prisma: PrismaService) {}

  async findById(id: string): Promise<ProductEntity | null> {
    const record = await this.prisma.product.findUnique({
      where: { id, deletedAt: null },
      include: { woodSpecies: true, category: true },
    });
    return record ? this.toEntity(record) : null;
  }
}
```

### 3.3 Database Module

```typescript
@Module({
  imports: [DatabaseModule],
  providers: [
    PrismaProductRepository,
    PrismaCategoryRepository,
    { provide: IProductRepository, useClass: PrismaProductRepository },
    { provide: ICategoryRepository, useClass: PrismaCategoryRepository },
  ],
  exports: [IProductRepository, ICategoryRepository],
})
export class ProductInfrastructureModule {}
```

**Key decisions:**
- Token-based DI — `@Inject(IProductRepository)` thay vì inject class trực tiếp → dễ swap mock khi test
- No generic repository — mỗi interface có method riêng cho domain đó
- Soft delete mặc định — mọi query đều filter `deletedAt: null`
- Entity mapping — repository chịu trách nhiệm map Prisma record → Domain entity

---

## 4. Domain Layer

### 4.1 Value Objects

Value objects — immutable, so sánh theo giá trị, không có identity riêng:

```typescript
// dimensions.vo.ts
export class Dimensions {
  constructor(
    readonly lengthCm: number,
    readonly widthCm: number,
    readonly heightCm: number,
  ) {
    if (lengthCm < 0 || widthCm < 0 || heightCm < 0) {
      throw new Error('Dimensions must be non-negative');
    }
  }

  get volumeCm3(): number {
    return this.lengthCm * this.widthCm * this.heightCm;
  }

  equals(other: Dimensions): boolean {
    return this.lengthCm === other.lengthCm
      && this.widthCm === other.widthCm
      && this.heightCm === other.heightCm;
  }
}
```

```typescript
// price.vo.ts
export class Price {
  constructor(
    readonly amount: number,
    readonly currency: string = 'VND',
  ) {
    if (amount < 0) throw new Error('Price cannot be negative');
  }

  equals(other: Price): boolean {
    return this.amount === other.amount && this.currency === other.currency;
  }
}
```

### 4.2 Domain Entities

Entities — có identity (ID), chứa business logic:

```typescript
export class ProductEntity {
  constructor(
    readonly id: string,
    readonly name: string,
    readonly slug: string,
    readonly price: Price,
    readonly woodSpecies: WoodSpecies,             // Enum loại gỗ
    readonly dimensions: Dimensions | null,
    readonly weightKg: number | null,
    readonly craftsmanship: string | null,
    readonly artisan: string | null,
    readonly finishType: string | null,
    readonly sku: string | null,
    readonly stockQuantity: number,
    readonly isOneOfAKind: boolean,
    readonly images: string[],
    readonly tags: string[],
    readonly categoryId: string | null,
    readonly isActive: boolean,
    readonly isFeatured: boolean,
    readonly createdAt: Date,
    readonly updatedAt: Date,
    readonly deletedAt: Date | null,
  ) {}

  get isAvailable(): boolean {
    return this.isActive
      && this.deletedAt === null
      && (this.isOneOfAKind || this.stockQuantity > 0);
  }

  canFulfill(quantity: number): boolean {
    if (!this.isAvailable) return false;
    if (this.isOneOfAKind) return quantity === 1;
    return this.stockQuantity >= quantity;
  }
}
```

### 4.3 Domain Exceptions

```typescript
// domain-error.base.ts
export abstract class DomainError extends Error {
  constructor(message: string, readonly code: string) {
    super(message);
    this.name = this.constructor.name;
  }
}

// product-not-found.exception.ts
export class ProductNotFoundException extends DomainError {
  constructor(id: string) {
    super(`Product not found: ${id}`, 'PRODUCT_NOT_FOUND');
  }
}
```

### 4.4 Supporting Types (Domain-level)

```typescript
// pagination.ts
export interface PaginationParams {
  page: number;
  limit: number;
}

export interface PaginatedResult<T> {
  data: T[];
  total: number;
  page: number;
  limit: number;
  totalPages: number;
}

// product-filter.ts
export interface ProductFilter {
  categoryId?: string;
  woodSpecies?: WoodSpecies;
  isActive?: boolean;
  isFeatured?: boolean;
  minPrice?: number;
  maxPrice?: number;
  minDimensions?: Dimensions;
  maxDimensions?: Dimensions;
}

// product-inputs.ts
export interface CreateProductInput {
  name: string;
  slug: string;
  price: Price;
  woodSpecies: WoodSpecies;
  categoryId?: string;
  dimensions?: Dimensions;
  weightKg?: number;
  craftsmanship?: string;
  artisan?: string;
  finishType?: string;
  sku?: string;
  stockQuantity?: number;
  isOneOfAKind?: boolean;
  images?: string[];
  tags?: string[];
  isFeatured?: boolean;
}

export interface UpdateProductInput extends Partial<CreateProductInput> {}
```

**Key decisions:**
- Entities là pure classes — không có decorator, không phụ thuộc framework
- Value objects validate khi tạo — fail fast, không bao giờ có state invalid
- Business logic trong entity — `isAvailable`, `canFulfill` — không leak ra service layer
- DomainError base class — thống nhất error handling, map sang HTTP/gRPC status code sau

---

## 5. Database Package (`packages/database/`)

Package này chỉ làm một việc: cung cấp PrismaClient được wrap trong NestJS module. Không biết gì về business entities.

### 5.1 `src/types.ts`

```typescript
export interface DbConfig {
  url: string;
  logLevel?: 'error' | 'warn' | 'info' | 'query';
  enableTransactions?: boolean;
}

export type TransactionClient = Omit<PrismaClient, '$connect' | '$disconnect' | '$on' | '$transaction' | '$use'>;
export type TransactionCallback<T> = (tx: TransactionClient) => Promise<T>;
```

### 5.2 `src/prisma.service.ts`

```typescript
@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() { await this.$connect(); }
  async onModuleDestroy() { await this.$disconnect(); }

  async transaction<T>(callback: TransactionCallback<T>): Promise<T> {
    return this.$transaction(async (tx) => callback(tx as unknown as TransactionClient));
  }
}
```

### 5.3 `src/database.module.ts`

```typescript
@Module({})
export class DatabaseModule {
  static forRoot(config: DbConfig): DynamicModule {
    const prismaProvider = {
      provide: PrismaService,
      useFactory: () => new PrismaService({
        datasources: { db: { url: config.url } },
        log: config.logLevel ? [config.logLevel] : ['error'],
      }),
    };

    return {
      module: DatabaseModule,
      providers: [prismaProvider],
      exports: [PrismaService],
      global: true,
    };
  }
}
```

### 5.4 `package.json`

```json
{
  "name": "@woodshop/database",
  "version": "0.1.0",
  "main": "src/index.ts",
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0"
  },
  "peerDependencies": {
    "prisma": "^5.0.0"
  }
}
```

**Key decisions:**
- DynamicModule `forRoot()` — nhận config từ service consumer, không hardcode DATABASE_URL
- `global: true` — import một lần, dùng ở mọi module trong service
- Transaction helper — wrapper `$transaction` để dễ dùng trong repository
- Prisma ở `peerDependencies` — service tự quyết định version Prisma, package chỉ cần client

---

## 6. Wiring & Bootstrap

### 6.1 `apps/product/src/app.module.ts`

```typescript
@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true, validate: validateEnv }),
    DatabaseModule.forRoot({
      url: process.env.DATABASE_URL!,
      logLevel: process.env.NODE_ENV === 'development' ? 'query' : 'error',
    }),
    ProductInfrastructureModule,
  ],
})
export class AppModule {}
```

### 6.2 `apps/product/src/main.ts`

```typescript
async function bootstrap() {
  const app = await NestFactory.create(AppModule, { logger: ['error', 'warn', 'log'] });
  const port = process.env.PORT || 3001;
  await app.listen(port);
  Logger.log(`Product service running on port ${port}`);
}
bootstrap();
```

### 6.3 `apps/product/.env`

```
DATABASE_URL="postgresql://woodshop:woodshop@localhost:5432/woodshop_product?schema=public"
NODE_ENV=development
PORT=3001
```

### 6.4 Bootstrap Flow

1. `ConfigModule` load và validate env vars
2. `DatabaseModule.forRoot()` khởi tạo PrismaService với DATABASE_URL
3. `ProductInfrastructureModule` register repositories với DI container
4. PrismaService tự `$connect()` khi module init
5. Application/Presentation layer (phase sau) sẽ inject repositories qua interface

---

## Out of Scope (Phase After)

- Application layer (CQRS commands/queries, handlers)
- Presentation layer (REST/gRPC controllers, DTOs, validators)
- docker-compose.yml (PostgreSQL, Redis, Kafka setup)
- Seed data script
- Unit/integration tests
- CI/CD pipeline
